{% extends 'base.html.twig' %}

{% block content %}
    <section class="container">
        <p class="mt-3">
            Si un compte existant est lié à votre email, alors un email a été envoyé contenant un lien pour pouvoir réinitialiser votre
            mot de passe.
            Ce lien expire dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
        </p>
        <p>Si vous ne recevez pas d'email, merci de vérifier vos spams ou <a href="{{ path('app_forgot_password_request') }}">Ré-éssayez</a>.</p>
    </section>
{% endblock %}
